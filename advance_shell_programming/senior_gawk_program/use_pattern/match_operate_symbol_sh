#!/bin/bash

clear

# 匹配操作符允许限制正则表达式记录中特定的数据字段
# 匹配操作符即波浪号 ~ 
# 需要指定匹配操作符，数据字段变量，还有要匹配的正则表达式


# 匹配操作符~ 将第二个数据字段与 正则表达式 /^2/ 进行比较
gawk 'BEGIN{FS=","} $2 ~ /^2/{print $0}' data

echo ++++++++++++++++++++++++++++++++
# 符号 ！ 否定正则表达式匹配
gawk 'BEGIN{FS=","} $2 !~  /^2/{print $0}' data



