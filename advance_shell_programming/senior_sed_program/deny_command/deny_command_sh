#!/bin/bash
clear

# sed 编辑器将命令应用于数据流中的每个文本行，或者应用于通过单个地址或者范围地址指定的行
# 也可以配置命令使其不应用到某些行

# ！用于否定命令， 它的意思是在命令通常被激活的地方不激活命令

sed -n '/header/!p' test

# next 命令中，有sed不能对数据文本最后一行执行操作，因为没有最后一行， 用 ！ 可以解决


sed '
!N;
s/three/change/
' test


echo 
# 倒叙输出文件

# 将第一行放入保留空间
# 将文本下一行放入模式空间
# 将保留空间追加到模式空间
# 重复2-4步
# 打印


sed -n '
1!G;
h;
p
' test
