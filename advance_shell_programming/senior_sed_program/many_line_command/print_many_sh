#!/bin/bash

clear

# 多行打印命令P  与多行删除命令类似，只打印模式空间中的第一行，包括至换行字符的所有字符
# 类似于 p 命令显示文本，并用 -n 选项 抑制脚本输出

sed -n  '/developer/{
N;
/developer\nkey/P
}' test2

# 发生多行匹配的时候 P命令只打印模式空间第一行
# 多行打印命令 P 的威力再于与 N D 命令结合使用

# D 命令有一个特有的功能，它强制sed编辑器返回到脚本的开始处，
# 在相同的模式空间（不是从数据流中读取新的文本行）重复执行命令，
# 通过在命令脚本中使用 N 可以有效的一步跨过模式空间，一起匹配多个行
# 接下来，使用 P 命令打印第一行，然后使用 D 命令删除第一行并循环回到脚本开始处
# 一旦回到脚本开始处，N 命令读取下一行，再次执行全部流程，知道达到数据流的结尾 

