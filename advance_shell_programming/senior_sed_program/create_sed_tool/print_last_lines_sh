#!/bin/bash
clear


# 打印最后一行 $ 符号表示数据流的最后一行
sed -n '$p' data
echo

# 打印最后几行
# 方法创建滚动窗口，滚动窗口是一种用于检测模式空间中文本快的常用方法,通常与N 命令结合使用
# N命令将文本的下一行追加到已经存在于模式空间的文本后面
# 假如模式空间中有一个10行文本块，可以通过$符号判断，是否处于数据流的末尾
# 如果不是最末尾可以继续讲更多的行添加到模式空间，但删除原始的行（使用D命令删除模式空间的第一行）

sed '{
:start;
$q;
N;
11,$D;
b start
}' /etc/passwd

# 首先查看行是否是数据流中的最后一行；若是，停止循环
# N将下一行追加到模式空间的当前行，如果当前行是第10行后面的行
# 11,$D 删除模式空间中的第一行
