#!/bin/bash
clear

# 在文本行之间添加空行
sed 'G' data

# 上述命令给最后一行也添加了空行

echo 
# 使用否定符号和末行符号来确保不会在最后一行添加空行
sed '$!G' data

# 对可能有空行的文件使用双倍行间距
# 如果原文件已经存在空行 可能会多加空行，不特别处理的话

# 解决方法是从数据流中删除空行，再插入空行
echo 

sed -n '/^$/d;$!G' data2

