#!/bin/bash
clear
# 删除行

# 1 删除连续空行
# 删除连续空行最简单的方法是使用地址范围查看数据流，sed编辑器对与指定地址相匹配的行执行命令
# 删除连续空行关键是创建一个地址范围；该范围包括一个空行一个非空行
# 如果sed编辑器遇到这个行，就不会删除行，如果不匹配则删除行

# 范围是 /./ 到 /^$/; 起始匹配任意字符，后面匹配空行  在此范围之内的内容不删除
sed '/./,/^$/!d' data2

echo ______________________________________________
# 2 删除开头的空行
# 地址范围以包含一个字符的行开始，一直到数据流的结尾； 此范围内的任何行都不会被删除， 反之开头的空行会被删除

sed '/./,$!d' data2

echo --------------------------------------------------------------

# 3 删除HTML标记































