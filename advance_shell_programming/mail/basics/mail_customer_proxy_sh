#!/bin/bash


# 现在，电子邮件已经从远程服务器到达了本地主机独立用户的邮箱；下一步是允许用户查看他们各自的邮件

# Linux邮件模型为为每个用户使用本地邮箱文件或者目录来保存用户信息
# MUA提供了一个让用户与邮箱互动的方法，以读取他们的信息


# MUA不接收信息，它只显示已经在邮箱中的信息
# MUA是提供一个用户与邮箱互动的方法，以读取他们的信息


# 大多数 MUA程序还提供邮件发送功能
# 前面说发送邮件是 MTA的任务
# MUA要发送邮件，大多数要利用SMTP中的智能主机功能；要么MUA将消息发送到本地的 MTA 进行发送




